# Smart Campus Planner - PRD (Product Requirements Document)

## 1. 문서 정보
- 문서명: Smart Campus Planner PRD
- 버전: v1.2 (최종)
- 작성일: 2026-08-25
- 최종 업데이트: 2026-08-28
- 작성 목적: 대학생 대상 일정/할 일 관리 웹앱의 요구사항 정의

---

## 2. 프로젝트 개요

### 2.1 프로젝트명
Smart Campus Planner

### 2.2 프로젝트 배경
대학생은 학과 과제, 동아리 활동, 개인적인 일정이 뒤섞여 관리 대상이 다양하고 산발적이다. 각기 다른 메모 앱, 캘린더, 메신저 등에 흩어져 있는 할 일들을 하나의 도구로 통합해서 관리할 수 있다면 학업과 대외활동, 개인 생활의 균형을 잡는 데 도움이 될 수 있다.

### 2.3 프로젝트 목적
- 대학생이 **학과 / 동아리 / 개인**의 모든 할 일을 한 화면에서 등록하고 관리할 수 있는 웹 기반 일정 관리 도구를 제공한다.
- 카테고리와 중요도, 마감일을 함께 관리하여 우선순위를 쉽게 파악하도록 돕는다.
- 완료율을 시각적으로 제공하여 실행 동기를 부여한다.
- 새로고침 또는 재방문 시에도 데이터가 유지되도록 하여 반복 사용이 가능한 실용적인 도구를 만든다.

---

## 3. 대상 사용자

### 3.1 주요 타깃
- **대학교 재학생** (학부생 기준, 대학원생도 포함 가능)
  - 학과 수업 과제, 팀 프로젝트, 시험 일정을 관리해야 하는 학생
  - 동아리/학생회 등 교내 활동에 참여하며 별도 일정 관리가 필요한 학생
  - 아르바이트, 자기계발, 약속 등 개인 일정을 함께 관리하고 싶은 학생

### 3.2 사용자 니즈
- 여러 영역(학과/동아리/개인)의 할 일을 앱을 옮겨 다니지 않고 한곳에서 확인하고 싶다.
- 무엇이 더 급하고 중요한지 한눈에 파악하고 싶다.
- 마감일을 놓치지 않고 챙기고 싶다.
- 내가 얼마나 할 일을 처리했는지(완료율) 확인하며 성취감을 느끼고 싶다.
- 별도 로그인/설치 없이 웹에서 가볍게 쓰고, 새로고침해도 입력한 내용이 사라지지 않길 원한다.

---

## 4. 주요 요구사항 (Functional Requirements)

### 4.1 할 일 추가
- 사용자는 새로운 할 일을 제목(내용)과 함께 등록할 수 있어야 한다.
- 할 일 추가 시 카테고리, 중요도, 마감일을 함께 입력할 수 있어야 한다.

### 4.2 카테고리 설정
- 각 할 일은 아래 3가지 카테고리 중 하나로 분류되어야 한다.
  - 학과
  - 동아리
  - 개인
- 카테고리별로 구분(색상, 라벨 등)이 가능해야 하며, 카테고리 기준으로 필터링/구분 조회가 가능해야 한다.

### 4.3 중요도 설정
- 각 할 일은 아래 3단계 중요도 중 하나를 가져야 한다.
  - 상
  - 중
  - 하
- 중요도는 목록에서 시각적으로 구분 가능해야 하며, 정렬 기준으로 활용할 수 있어야 한다.

### 4.4 마감일 지정
- 사용자는 할 일마다 마감일(날짜)을 지정할 수 있어야 한다.
- 마감일 기준 정렬 및 임박한 마감일 확인이 가능해야 한다.

### 4.5 완료 체크 및 삭제
- 사용자는 할 일 목록에서 완료 여부를 체크(토글)할 수 있어야 한다.
- 사용자는 더 이상 필요 없는 할 일을 삭제할 수 있어야 한다.
- 완료된 항목은 목록에서 시각적으로 구분되어야 한다(예: 취소선, 흐림 처리 등).

### 4.6 전체 완료율 표시
- 전체 할 일 대비 완료된 할 일의 비율을 계산하여 화면에 표시해야 한다.
- 완료율은 할 일이 추가/삭제/완료 체크될 때마다 실시간으로 갱신되어야 한다.

### 4.7 데이터 저장 및 유지
- 사용자가 등록한 할 일 데이터는 새로고침하거나 브라우저를 재방문해도 유지되어야 한다.
- 별도의 로그인 없이도 개인 브라우저 환경에서 데이터가 보존되어야 한다.

---

## 5. 비기능 요구사항 (Non-Functional Requirements)

- **접근성**: 별도 설치 없이 웹 브라우저에서 바로 사용할 수 있어야 한다.
- **반응성**: 데스크톱뿐 아니라 모바일/태블릿 환경에서도 사용 가능한 화면 구성을 고려한다.
- **직관성**: 대학생이 별도 학습 없이 바로 사용할 수 있는 단순하고 직관적인 UI/UX를 제공한다.
- **데이터 안정성**: 사용자의 입력 데이터가 예기치 않게 소실되지 않아야 한다(새로고침 시 유지 포함).

---

## 6. MVP(Minimum Viable Product) 범위

### 6.1 MVP 포함 기능
1. 할 일 추가 (제목 입력)
2. 카테고리 선택 (학과 / 동아리 / 개인)
3. 중요도 선택 (상 / 중 / 하)
4. 마감일 지정 (날짜 선택)
5. 할 일 목록 조회 (등록된 항목 리스트로 표시)
6. 완료 체크 기능 (완료/미완료 토글)
7. 할 일 삭제 기능
8. 전체 완료율 표시 (완료 개수 / 전체 개수 기반 %)
9. 새로고침 후에도 데이터 유지 (브라우저 저장소 기반)

### 6.2 MVP 제외 범위 (추후 고도화 검토)
- 회원가입/로그인 및 다중 사용자 계정, 서버/DB 연동
- 여러 기기 간 실시간 데이터 동기화 (단, JSON 내보내기/가져오기를 통한 수동 이전은 v1.2에서 구현됨)
- 마감일 알림/푸시 알림 기능
- 통계(차트) 기능
- 반복 일정 ※ 캘린더 뷰는 v1.1에서, 검색·필터·수정 기능은 v1.2에서 구현됨
- 협업 기능(동아리원과 할 일 공유 등)

---

## 7. 성공 기준 (Success Criteria)
- 사용자가 학과/동아리/개인 할 일을 모두 하나의 화면에서 추가하고 관리할 수 있다.
- 카테고리, 중요도, 마감일 정보가 각 할 일 항목에 정확히 표시된다.
- 완료 체크 시 완료율이 즉시 갱신되어 표시된다.
- 브라우저를 새로고침해도 이전에 등록한 할 일 목록과 완료 상태가 그대로 유지된다.

---

## 8. 향후 논의가 필요한 사항
- 데이터 저장 방식(로컬 저장 vs 서버 저장)에 대한 세부 기술 결정
- 카테고리 확장 가능성 (예: 사용자 정의 카테고리 추가 여부)
- 마감일 임박 항목에 대한 강조 표시 기준

---

## 9. 확장 요구사항 (v1.1) - 기간 지정 & 달력 보기

> MVP(v1.0, 1~3단계)가 완성된 이후 추가로 요청된 기능이다. 기존 MVP 기능(할 일 추가, 카테고리/중요도, 완료 체크, 삭제, 완료율, 저장)은 그대로 유지한 채 아래 기능을 확장한다.

### 9.1 배경
기존에는 할 일마다 "마감일" 하나만 지정할 수 있었다. 하지만 실제로는 과제나 프로젝트처럼 "언제부터 언제까지"라는 기간을 갖는 할 일이 많고, 이런 할 일들을 목록(리스트)이 아니라 달력 위에서 한눈에 파악하고 싶다는 요구가 있다.

### 9.2 추가 요구사항
1. **기간 지정 (시작일 ~ 마감일)**
   - 할 일 등록 시 마감일 하나만이 아니라, 시작일과 마감일(종료일)을 함께 지정할 수 있어야 한다.
   - 시작일을 입력하지 않으면 마감일만 있는 할 일(기존 방식)로도 등록할 수 있어야 한다(하위 호환).
   - 시작일은 마감일보다 늦을 수 없다는 최소한의 입력 검증이 있어야 한다.
2. **보기 방식 전환 (리스트 뷰 ↔ 달력 뷰)**
   - 화면 상단(또는 눈에 띄는 위치)에 "리스트 보기 / 달력 보기"를 전환하는 버튼(또는 탭)을 제공한다.
   - 리스트 보기는 기존 목록 화면(제목/카테고리/중요도/기간/완료 체크/삭제)을 그대로 유지한다.
   - 달력 보기는 한 달 단위의 달력 화면으로 전환되며, 완료 체크·삭제 등 기존 조작은 달력 보기에서도 가능해야 한다.
3. **달력 위 기간 시각화**
   - 각 할 일은 시작일부터 마감일까지의 날짜 범위에 걸쳐 달력 위에 막대(바) 형태 등으로 표시되어야 한다.
   - 카테고리(학과/동아리/개인)는 달력에서도 색상으로 구분되어야 한다.
   - 완료된 할 일은 달력에서도 시각적으로 구분(예: 흐림, 취소선)되어야 한다.
   - 하루짜리 할 일(시작일=마감일, 또는 마감일만 있는 할 일)은 해당 날짜 한 칸에 표시되어야 한다.

### 9.3 이번 확장에서 제외하는 것
- 여러 달을 한 번에 보여주는 뷰(연간 보기 등)
- 달력에서 날짜를 드래그해서 새 할 일을 만드는 기능
- 주간(week) 단위 보기
- 이 확장 범위는 기존 MVP 제외 범위(로그인/서버 연동, 알림, 필터/검색/통계, 반복 일정, 협업 기능 등)를 그대로 계승하며 포함하지 않는다.

---

## 10. 확장 요구사항 (v1.2) - UX 고도화 & 완성도 개선

> v1.1(달력 보기)이 완성된 이후 추가로 요청된 기능이다. 기존 v1.0/v1.1 기능은 그대로 유지한 채 아래 기능을 확장한다.

### 10.1 배경
달력과 목록을 별도 화면으로 전환하는 방식보다 한 화면에서 동시에 볼 수 있으면 더 직관적이다. 또한 검색·필터·정렬, 인라인 수정, D-day 표시, 다크 모드, 데이터 내보내기/가져오기, 설정 커스터마이징 등 실용성을 높이는 기능이 추가로 요구되었다.

### 10.2 추가 요구사항

1. **달력 + 목록 동시 보기 (분할 레이아웃)**
   - 데스크톱에서는 좌측 달력과 우측 목록을 한 화면에서 동시에 표시한다.
   - 태블릿·모바일에서는 탭(목록 / 달력) 버튼으로 전환한다.

2. **달력 ↔ 목록 연동**
   - 목록에서 항목을 클릭하면 달력이 해당 월로 이동하고 해당 셀이 강조된다.
   - 달력의 막대(바)를 클릭하면 목록에서 해당 항목이 선택(강조)되고 스크롤된다.

3. **검색**
   - 제목 또는 메모 내용으로 실시간 검색이 가능해야 한다.
   - 검색 결과는 목록과 달력에 동시에 반영된다.

4. **필터 & 정렬**
   - 카테고리, 중요도, 완료 여부 기준으로 필터링할 수 있어야 한다.
   - 등록순, 마감일순, 중요도순으로 정렬할 수 있어야 한다.

5. **인라인 수정**
   - 목록에서 수정 버튼을 누르면 해당 항목이 편집 모드로 전환되어 모든 필드를 바로 수정할 수 있어야 한다.

6. **메모 필드**
   - 할 일 등록/수정 시 선택적으로 메모를 입력할 수 있어야 한다.

7. **D-day 표시 및 긴급도 강조**
   - 각 할 일에 마감일까지 남은 날수를 D-day 형태(D-3, D-day, D+1 등)로 표시한다.
   - 임박(3일 이내), 당일(D-day), 초과(D+) 여부를 색상으로 구분한다.

8. **카테고리별 진행률**
   - 전체 완료율 외에 카테고리별 완료 현황을 진행률 바로 표시한다.

9. **다크 모드**
   - 버튼 하나로 다크 모드를 전환할 수 있어야 하며, 설정이 브라우저에 저장되어야 한다.

10. **JSON 내보내기 / 가져오기**
    - 할 일 데이터와 설정(카테고리·중요도)을 JSON 파일로 내보낼 수 있어야 한다.
    - JSON 파일을 가져오면 할 일과 설정이 동일하게 복원되어야 한다(다기기 이전 지원).

11. **카테고리 · 중요도 커스터마이징**
    - 설정 모달에서 카테고리와 중요도 항목을 자유롭게 추가하거나 삭제할 수 있어야 한다.
    - 기본값(학과/동아리/개인, 상/중/하)은 초기값으로 제공하되 수정 가능하다.
    - 각 항목에는 팔레트에서 자동으로 색상이 지정된다.

### 10.3 이번 확장에서 제외하는 것
- 회원가입/로그인, 서버·DB 연동, 실시간 다기기 동기화
- 마감일 알림/푸시 알림
- 통계(차트) 기능
- 반복 일정, 협업 기능
- 달력에서 날짜를 드래그해서 새 할 일을 만드는 기능

# Smart Campus Planner - 단계별 구현 요청 프롬프트 (PROMPTS)

## 문서 정보
- 문서명: Smart Campus Planner 구현 요청 프롬프트 모음
- 버전: v1.1
- 작성일: 2026-08-25
- 참고 문서: PRD.md, TASKS.md
- 작성 목적: TASKS.md의 T1~T5 순서에 맞춰, 각 단계를 AI(또는 개발자)에게 실제로 요청할 때 사용할 프롬프트 문구를 미리 정리해 둔 문서

> 본 문서는 "요청 문구 모음"이며, 이 문서 자체를 통해 코드를 작성하거나 수정하지는 않는다.
> 아래 프롬프트는 한 번에 하나씩만 사용한다. 즉, T1 프롬프트를 먼저 사용해 결과를 확인한 뒤, 이상이 없을 때 T2 프롬프트를 사용하고, T2 확인 후에 T3 프롬프트를 사용하는 순서를 지킨다. T1~T3(MVP)가 모두 완료된 뒤 T4, T5(확장 기능) 순서로 이어서 진행한다.
>
> 참고: 사용자가 요청한 "시작일~마감일 지정 + 리스트/달력 전환 보기"는 서로 다른 두 가지 작업(① 기간 데이터 추가, ② 보기 방식 전환 및 달력 시각화)이므로, 지금까지의 방식과 동일하게 T4(기간 지정)와 T5(리스트-달력 전환 & 시각화)로 나누어 순서대로 진행한다.

---

## 사용 방법 안내 (쉬운 설명)
1. 아래 "T1 프롬프트"를 그대로 복사해서 AI에게 전달한다.
2. AI가 만든 결과물을 "확인 체크리스트"에 따라 직접 눈으로 확인한다.
3. 문제가 없으면 T2 프롬프트로 넘어가고, 문제가 있으면 무엇이 잘못되었는지 설명해서 같은 단계(T1)를 다시 요청한다.
4. T2, T3도 같은 방식으로 순서대로 진행한다.

---

## T1 프롬프트: 할 일 등록 & 목록 화면 만들기

### 이 단계의 목표 (쉬운 설명)
할 일 제목과 카테고리, 중요도, 마감일을 입력해서 등록하면, 그 내용이 목록으로 화면에 보이는 웹페이지를 만드는 단계다.

### 요청 프롬프트
```
Smart Campus Planner라는 대학생용 할 일 관리 웹앱을 만들고 있어.
지금은 전체 기능 중 "1단계: 할 일 등록 & 목록 화면 만들기"만 구현해줘.

이번 단계에서 필요한 기능은 다음과 같아.
1. 할 일 제목을 입력할 수 있는 입력창
2. 카테고리를 선택하는 기능 (학과 / 동아리 / 개인 중 하나 선택)
3. 중요도를 선택하는 기능 (상 / 중 / 하 중 하나 선택)
4. 마감일을 선택하는 기능 (날짜 선택)
5. "추가" 버튼을 누르면 입력한 내용이 목록에 새 항목으로 추가되어 화면에 보이기
6. 목록의 각 항목에는 제목, 카테고리, 중요도, 마감일이 모두 표시되어야 하고,
   카테고리와 중요도는 색상이나 라벨로 구분되어 한눈에 알아볼 수 있게 해줘.

아직 완료 체크, 삭제, 완료율 표시, 새로고침 후 데이터 유지 기능은 만들지 마.
이번 단계는 "입력하면 목록에 나타난다"까지만 동작하면 충분해.

HTML, CSS, JavaScript로 구현해줘. 별도의 서버나 로그인은 필요 없고,
대학생이 별도 설치 없이 바로 웹 브라우저에서 사용할 수 있도록 간단하고 보기 편한 화면으로 만들어줘.
```

### 확인 체크리스트 (결과물을 이렇게 확인하세요)
- [ ] 할 일 제목, 카테고리, 중요도, 마감일을 입력하는 항목이 화면에 모두 보이는가?
- [ ] "추가" 버튼을 누르면 입력한 내용이 목록에 새로 나타나는가?
- [ ] 목록에서 각 할 일의 카테고리와 중요도를 색상이나 표시로 구분해서 알아볼 수 있는가?
- [ ] 목록에 마감일이 함께 표시되는가?
- [ ] 여러 개의 할 일을 연속으로 추가해도 목록에 잘 쌓이는가?

### 다음 단계로 넘어가는 조건
위 체크리스트를 모두 만족하면 T2 프롬프트로 진행한다.

---

## T2 프롬프트: 완료 체크 · 삭제 · 완료율 표시

### 이 단계의 목표 (쉬운 설명)
1단계에서 만든 목록에서 할 일을 완료로 체크하거나 삭제할 수 있게 만들고, 전체 할 일 중 몇 %를 끝냈는지 보여주는 단계다.

### 요청 프롬프트
```
지금 만들어진 Smart Campus Planner 화면(할 일 제목/카테고리/중요도/마감일을 입력하고
목록으로 보여주는 기능)은 그대로 유지하면서,
이번에는 "2단계: 완료 체크 · 삭제 · 완료율 표시" 기능을 추가해줘.

이번 단계에서 필요한 기능은 다음과 같아.
1. 목록의 각 할 일 옆에 체크박스(또는 완료 버튼)를 추가해서,
   누르면 완료 상태로 바뀌고 다시 누르면 완료가 취소되게 해줘.
2. 완료된 할 일은 취소선이나 흐리게 표시하는 등 미완료 항목과 구분되게 보여줘.
3. 목록의 각 할 일 옆에 삭제 버튼을 추가해서, 누르면 그 항목이 목록에서 없어지게 해줘.
4. 화면에 "전체 완료율"을 보여줘 (예: 완료한 할 일 개수 / 전체 할 일 개수, 퍼센트로 표시).
   할 일을 추가하거나, 완료 체크하거나, 삭제할 때마다 완료율 숫자가 바로바로 업데이트되어야 해.

1단계에서 만든 입력 기능과 목록 표시 기능은 그대로 유지해줘.
아직 새로고침 후에도 데이터가 남아있게 하는 저장 기능은 만들지 않아도 돼.
```

### 확인 체크리스트 (결과물을 이렇게 확인하세요)
- [ ] 목록에서 체크박스(또는 완료 버튼)를 누르면 완료 상태로 표시가 바뀌는가?
- [ ] 완료된 항목이 미완료 항목과 다르게(취소선, 흐림 등) 구분되어 보이는가?
- [ ] 삭제 버튼을 누르면 해당 항목이 목록에서 사라지는가?
- [ ] 화면에 전체 완료율(%)이 보이는가?
- [ ] 할 일을 추가/체크/삭제할 때마다 완료율 숫자가 바로 바뀌는가?
- [ ] 1단계에서 만든 입력 및 목록 기능이 그대로 잘 동작하는가?

### 다음 단계로 넘어가는 조건
위 체크리스트를 모두 만족하면 T3 프롬프트로 진행한다.

---

## T3 프롬프트: 새로고침해도 유지되는 저장 기능

### 이 단계의 목표 (쉬운 설명)
지금까지 만든 할 일 목록과 완료 상태가 새로고침하거나 브라우저를 껐다 켜도 사라지지 않도록 저장하고, 전체 화면을 깔끔하게 마무리하는 단계다.

### 요청 프롬프트
```
지금 만들어진 Smart Campus Planner의 모든 기능(할 일 추가, 카테고리/중요도/마감일 설정,
목록 표시, 완료 체크, 삭제, 완료율 표시)은 그대로 유지하면서,
이번에는 "3단계: 새로고침해도 유지되는 저장 기능"을 추가해줘.

이번 단계에서 필요한 기능은 다음과 같아.
1. 할 일을 추가하거나, 완료 체크하거나, 삭제할 때마다 그 내용이 브라우저에 저장되게 해줘.
   (별도의 서버나 로그인 없이, 이 브라우저 안에서만 저장되고 불러와지면 돼.)
2. 웹페이지를 새로고침하거나 껐다가 다시 열어도, 이전에 등록했던 할 일 목록과
   완료 상태, 카테고리, 중요도, 마감일 정보가 그대로 남아있게 해줘.
3. 전체적으로 화면 디자인과 배치를 한 번 더 다듬어서,
   컴퓨터뿐 아니라 휴대폰 화면에서도 보기 편하도록 정리해줘.

기존에 만든 입력, 목록, 완료 체크, 삭제, 완료율 기능은 지금과 동일하게 계속 동작해야 해.
이 단계까지 끝나면 처음에 계획했던 대학생용 할 일 관리 웹앱의 기본 기능이 모두 완성되는 거야.
```

### 확인 체크리스트 (결과물을 이렇게 확인하세요)
- [ ] 할 일을 추가한 뒤 새로고침해도 목록에 그대로 남아있는가?
- [ ] 할 일을 완료 체크한 뒤 새로고침해도 완료 상태가 유지되는가?
- [ ] 할 일을 삭제한 뒤 새로고침해도 삭제된 상태가 유지되는가?(다시 나타나지 않는가?)
- [ ] 브라우저 탭을 완전히 닫았다가 다시 열어도 데이터가 남아있는가?
- [ ] 휴대폰 크기의 화면(또는 브라우저 창을 좁혔을 때)에서도 화면이 깨지지 않고 보기 편한가?
- [ ] 1단계와 2단계에서 만든 모든 기능(추가/카테고리/중요도/마감일/완료 체크/삭제/완료율)이 여전히 잘 동작하는가?

### 완료 조건
위 체크리스트를 모두 만족하면 PRD.md에 정의된 MVP 범위가 모두 구현된 것이며, Smart Campus Planner의 첫 완성 버전이 완료된 것으로 본다.

---

## T4 프롬프트: 시작일 ~ 마감일 기간 지정

### 이 단계의 목표 (쉬운 설명)
지금까지는 할 일마다 마감일 하나만 정할 수 있었는데, 이제 "언제부터(시작일) 언제까지(마감일)"인지 기간으로 정할 수 있게 바꾸는 단계다. 아직 달력 화면은 만들지 않고, 지금의 목록 화면에서 기간 정보가 잘 보이는 것까지만 다룬다.

### 요청 프롬프트
```
지금 만들어진 Smart Campus Planner의 모든 기능(할 일 추가, 카테고리/중요도 설정,
목록 표시, 완료 체크, 삭제, 완료율 표시, 새로고침 후 데이터 유지)은 그대로 유지하면서,
이번에는 "4단계: 시작일 ~ 마감일 기간 지정" 기능을 추가해줘.

이번 단계에서 필요한 기능은 다음과 같아.
1. 할 일을 등록할 때, 기존의 마감일 입력란 옆에 "시작일" 입력란을 추가해줘.
2. 시작일은 선택 사항으로 남겨줘. 즉, 시작일을 입력하지 않고 마감일만 입력해도
   지금처럼 정상적으로 등록되어야 해.
3. 시작일을 마감일보다 늦은 날짜로 입력하면 "시작일은 마감일보다 늦을 수 없어요" 같은
   안내 메시지를 보여주고 등록되지 않게 해줘.
4. 목록에 표시되던 마감일 정보를, 시작일이 있는 경우에는 "시작일 ~ 마감일" 형태로,
   시작일이 없는 경우에는 지금처럼 마감일만 보이도록 표시해줘.
5. 기존에 저장된 할 일 데이터(마감일만 있던 데이터)도 문제없이 계속 보여야 해.

이번 단계에서는 아직 달력 화면이나 리스트-달력 전환 기능은 만들지 마.
지금의 목록(리스트) 화면 안에서 기간 정보만 잘 반영되면 충분해.
기존에 만든 다른 모든 기능은 지금과 동일하게 계속 동작해야 해.
```

### 확인 체크리스트 (결과물을 이렇게 확인하세요)
- [ ] 할 일 등록 폼에 시작일 입력란이 새로 보이는가?
- [ ] 시작일 없이 마감일만 입력해도 정상적으로 등록되는가?
- [ ] 시작일을 마감일보다 늦게 입력하면 안내 메시지가 뜨고 등록이 안 되는가?
- [ ] 목록에서 기간이 있는 할 일은 "시작일 ~ 마감일" 형태로 보이는가?
- [ ] 마감일만 있는 할 일은 기존처럼 마감일만 표시되는가?
- [ ] 새로고침해도 시작일 정보가 그대로 유지되는가?
- [ ] 기존 기능(카테고리/중요도/완료 체크/삭제/완료율)이 여전히 잘 동작하는가?

### 다음 단계로 넘어가는 조건
위 체크리스트를 모두 만족하면 T5 프롬프트로 진행한다.

---

## T5 프롬프트: 리스트 ↔ 달력 전환 & 기간 시각화

### 이 단계의 목표 (쉬운 설명)
화면에서 "목록 보기"와 "달력 보기"를 버튼으로 바꿔가며 볼 수 있게 만들고, 달력 보기에서는 각 할 일이 시작일부터 마감일까지 걸쳐 막대(바) 모양으로 표시되도록 만드는 단계다.

### 요청 프롬프트
```
지금 만들어진 Smart Campus Planner의 모든 기능(할 일 추가, 시작일~마감일 기간 지정,
카테고리/중요도 설정, 목록 표시, 완료 체크, 삭제, 완료율 표시, 새로고침 후 데이터 유지)은
그대로 유지하면서, 이번에는 "5단계: 리스트 ↔ 달력 전환 & 기간 시각화" 기능을 추가해줘.

이번 단계에서 필요한 기능은 다음과 같아.
1. 화면 상단에 "목록 보기 / 달력 보기"를 전환할 수 있는 버튼(또는 탭)을 추가해줘.
2. "목록 보기"를 선택하면 지금까지 사용하던 목록 화면이 그대로 보여야 해.
3. "달력 보기"를 선택하면 이번 달 달력이 나타나고, 각 할 일이 시작일부터 마감일까지의
   날짜 범위에 걸쳐 막대(바) 형태로 달력 위에 표시되어야 해.
4. 시작일 없이 마감일만 있는 할 일(하루짜리 할 일)은 그 날짜 한 칸에 표시해줘.
5. 달력에서도 카테고리(학과/동아리/개인)를 색상으로 구분해줘.
6. 달력에서도 완료된 할 일은 흐리게 표시하거나 취소선을 넣는 등 미완료 할 일과
   구분되게 해줘.
7. 달력 보기에서도 할 일을 완료 체크하거나 삭제할 수 있게 해줘(예: 막대를 클릭하면
   해당 할 일을 완료 처리하거나 삭제할 수 있는 방법을 제공).
8. "달력 보기"에서도 이전 달/다음 달로 이동할 수 있는 버튼을 추가해줘.

목록 보기의 기존 기능(추가, 완료 체크, 삭제, 완료율, 새로고침 후 저장)은
지금과 동일하게 계속 동작해야 해. 여러 달을 한 번에 보여주는 기능이나,
달력에서 날짜를 드래그해서 새 할 일을 만드는 기능, 주간 단위 보기는 만들지 않아도 돼.
```

### 확인 체크리스트 (결과물을 이렇게 확인하세요)
- [ ] "목록 보기 / 달력 보기" 버튼(또는 탭)으로 화면 전환이 잘 되는가?
- [ ] 목록 보기 화면은 기존과 동일하게 정상적으로 보이는가?
- [ ] 달력 보기에서 이번 달 달력이 올바르게 표시되는가?
- [ ] 시작일~마감일이 있는 할 일이 달력 위에 해당 기간에 걸쳐 막대로 표시되는가?
- [ ] 마감일만 있는(하루짜리) 할 일이 해당 날짜 한 칸에 표시되는가?
- [ ] 달력에서도 카테고리별 색상 구분이 잘 보이는가?
- [ ] 달력에서도 완료된 할 일이 미완료 할 일과 시각적으로 구분되는가?
- [ ] 달력 보기에서 완료 체크와 삭제가 가능한가?
- [ ] 이전 달/다음 달 이동 버튼이 정상 동작하는가?
- [ ] 새로고침해도 달력에 표시되는 내용이 유지되는가?
- [ ] 기존 목록 기능(추가/완료 체크/삭제/완료율/저장)이 여전히 잘 동작하는가?

### 완료 조건
위 체크리스트를 모두 만족하면 PRD.md 9장에 정의된 확장 요구사항(v1.1: 기간 지정 & 달력 보기)이 모두 구현된 것으로 본다.

---

## 참고 사항
- 각 프롬프트는 이전 단계에서 만든 기능을 "그대로 유지"하면서 새 기능만 추가하도록 요청하는 방식으로 작성되어 있다. 이는 TASKS.md에서 설명한 "앞 단계의 결과를 바탕으로 다음 단계를 진행"하는 구조를 따른 것이다.
- 확인 체크리스트 항목 중 하나라도 만족하지 않으면, 다음 단계로 넘어가지 말고 어떤 부분이 기대와 다른지 구체적으로 설명하여 같은 단계의 프롬프트를 다시 요청한다.
- PRD.md의 "MVP 제외 범위"에 해당하는 기능(로그인, 서버 연동, 알림, 필터/검색/통계, 수정 기능, 협업 기능 등)은 T1~T5 어느 단계에도 포함하지 않는다.
- PRD.md 9.3에 명시된 확장 제외 범위(연간 보기, 날짜 드래그로 할 일 생성, 주간 보기)는 T5 단계에도 포함하지 않는다.

# Smart Campus Planner - 단계별 작업 계획 (TASKS)

## 문서 정보
- 문서명: Smart Campus Planner 구현 작업 분할 계획
- 버전: v1.2
- 작성일: 2026-08-25
- 최종 업데이트: 2026-08-28
- 참고 문서: PRD.md
- 작성 목적: PRD에 정의된 요구사항과 MVP 범위를 한 번에 구현하지 않고, 서로 연관된 기능끼리 묶어 단계별로 나누어 순차적으로 구현하기 위함
  - 1~3단계: PRD 6장(MVP, v1.0) 구현
  - 4~5단계: PRD 9장(확장 요구사항, v1.1 - 기간 지정 & 달력 보기) 구현
  - 6단계: PRD 10장(확장 요구사항, v1.2 - UX 고도화 & 완성도 개선) 구현

> 본 문서는 계획 문서이며, 실제 프로그램 코드는 아직 작성하지 않는다.

---

## 전체 진행 순서 요약

| 단계 | 이름 | 핵심 한 줄 설명 |
|---|---|---|
| 1단계 | 할 일 등록 & 목록 화면 만들기 | 할 일을 정보와 함께 입력하고, 목록으로 볼 수 있는 기본 틀을 만든다 |
| 2단계 | 완료 체크 · 삭제 · 완료율 표시 | 등록된 할 일을 관리(체크/삭제)하고, 전체 진행률을 보여준다 |
| 3단계 | 새로고침해도 유지되는 저장 기능 | 입력한 내용이 브라우저를 껐다 켜도 사라지지 않도록 만든다 |
| 4단계 (v1.1 확장) | 시작일 ~ 마감일 기간 지정 | 마감일 하나만 있던 것을 "언제부터 언제까지"로 지정할 수 있게 바꾼다 |
| 5단계 (v1.1 확장) | 달력 + 목록 분할 보기 & 기간 시각화 | 좌측 달력과 우측 목록을 한 화면에 동시에 보여주고, 달력 위에 할 일 기간을 막대로 표시한다 |
| 6단계 (v1.2 확장) | UX 고도화 & 완성도 개선 | 검색·필터·정렬, 인라인 수정, 메모, D-day, 다크 모드, 설정 커스터마이징, 내보내기/가져오기를 추가한다 |

앞 단계에서 만든 "화면"과 "데이터 구조"를 다음 단계에서 그대로 이어받아 기능을 덧붙이는 방식으로 진행한다. 즉, 1단계 없이는 2단계를 만들 수 없고, 2단계 없이는 3단계의 저장 대상이 완전해지지 않는 구조다. 4단계·5단계는 1~3단계로 완성된 MVP(v1.0) 위에 추가되는 확장 기능(v1.1)이며, 6단계는 5단계의 분할 레이아웃 위에 UX 기능들을 덧붙이는 v1.2 확장이다.

---

## 1단계: 할 일 등록 & 목록 화면 만들기

### 이 단계에서 하는 일 (쉬운 설명)
가장 먼저, 사용자가 "무엇을, 어떤 종류로, 얼마나 중요하게, 언제까지" 해야 하는지를 입력할 수 있는 입력 화면을 만든다. 그리고 입력한 할 일들이 화면 아래에 목록 형태로 차곡차곡 쌓여 보이도록 만든다. 아직 완료 체크나 삭제, 저장 기능은 없고, "입력하면 목록에 나타난다"까지만 동작하는 단계다.

### 포함되는 PRD 요구사항
- 할 일 추가 (제목 입력)
- 카테고리 설정 (학과 / 동아리 / 개인 중 선택)
- 중요도 설정 (상 / 중 / 하 중 선택)
- 마감일 지정 (날짜 선택)
- 등록된 할 일을 목록 형태로 화면에 보여주기

### 왜 이 기능들을 함께 묶었는가
할 일 제목, 카테고리, 중요도, 마감일은 모두 "할 일 하나를 등록할 때 동시에 입력받아야 하는 정보"이기 때문에 따로 떼어 만들기 어렵다. 이 정보들을 한 번에 입력받아 목록에 표시하는 화면을 먼저 완성해야, 이후 단계에서 "이 목록에 있는 항목을 체크하거나 지우는" 기능을 자연스럽게 이어서 만들 수 있다.

### 이 단계가 끝났을 때 확인할 수 있는 모습
- 할 일 제목, 카테고리(학과/동아리/개인), 중요도(상/중/하), 마감일을 입력하는 폼(양식)이 화면에 보인다.
- "추가" 버튼을 누르면 입력한 내용이 목록에 새 항목으로 추가되어 보인다.
- 목록의 각 항목에서 카테고리, 중요도, 마감일을 구분해서 볼 수 있다(예: 색상이나 표시 라벨로 구분).
- (아직) 새로고침하면 목록은 사라져도 되고, 완료 체크/삭제 기능은 없어도 된다.

---

## 2단계: 완료 체크 · 삭제 · 완료율 표시

### 이 단계에서 하는 일 (쉬운 설명)
1단계에서 만든 목록에 "체크박스"와 "삭제 버튼"을 추가해서, 할 일을 다 하면 체크로 표시하고 더 이상 필요 없는 항목은 지울 수 있게 만든다. 그리고 화면 상단이나 눈에 잘 띄는 곳에 "전체 할 일 중 몇 %를 완료했는지" 알려주는 완료율을 보여준다.

### 포함되는 PRD 요구사항
- 완료 체크 기능 (완료/미완료 토글)
- 삭제 기능
- 완료된 항목의 시각적 구분 (예: 취소선, 흐림 처리)
- 전체 완료율 표시 (완료 개수 / 전체 개수 기반 %, 실시간 갱신)

### 왜 이 기능들을 함께 묶었는가
완료 체크, 삭제, 완료율은 모두 "1단계에서 만든 목록 데이터를 다루는" 기능이라는 공통점이 있다. 목록에 항목이 있어야 체크하고 지울 대상이 생기고, 체크/삭제 결과가 곧바로 완료율 계산에 반영되어야 하므로 세 기능은 서로 맞물려 동작한다. 따라서 목록 관리 기능들을 한 단계로 묶어 함께 구현하는 것이 자연스럽다.

### 이 단계가 끝났을 때 확인할 수 있는 모습
- 목록의 각 할 일 옆에 체크박스(또는 완료 버튼)가 있어서 누르면 완료 상태로 바뀐다.
- 완료된 할 일은 글자에 줄이 그어지거나 흐리게 표시되는 등 미완료 항목과 구분되어 보인다.
- 각 할 일 옆에 삭제 버튼이 있어서 누르면 목록에서 사라진다.
- 화면에 "전체 완료율(예: 3/5 완료, 60%)"이 표시되고, 체크하거나 삭제할 때마다 숫자가 바로 갱신된다.

---

## 3단계: 새로고침해도 유지되는 저장 기능

### 이 단계에서 하는 일 (쉬운 설명)
지금까지 만든 할 일 목록, 완료 상태, 카테고리, 중요도, 마감일 정보가 브라우저를 새로고침하거나 껐다가 다시 켜도 그대로 남아있도록, 내용을 브라우저에 저장하고 다시 불러오는 기능을 추가한다. 또한 전체적으로 화면을 다듬어(디자인/배치 정리) 실제로 편하게 쓸 수 있는 완성된 형태로 마무리한다.

### 포함되는 PRD 요구사항
- 새로고침 후에도 데이터 유지 (브라우저 저장소 기반)
- 로그인 없이 개인 브라우저 환경에서 데이터 보존
- (마무리 다듬기) 반응형 화면 구성, 직관적인 UI 정리 등 비기능 요구사항 반영

### 왜 이 기능들을 함께 묶었는가
저장 기능은 "1단계와 2단계에서 다루는 모든 데이터(할 일 제목, 카테고리, 중요도, 마감일, 완료 여부)"를 대상으로 동작해야 의미가 있다. 즉, 저장할 데이터의 형태(할 일 목록 구조)와 그 데이터를 바꾸는 동작(추가/체크/삭제)이 앞 단계에서 이미 확정되어 있어야, 이 단계에서 "그 상태를 그대로 저장했다가 불러오는" 기능을 안정적으로 만들 수 있다. 그래서 저장 기능은 항상 마지막 단계에 두는 것이 자연스럽다.

### 이 단계가 끝났을 때 확인할 수 있는 모습
- 할 일을 추가하거나, 체크하거나, 삭제한 뒤 브라우저를 새로고침해도 방금 전과 동일한 목록과 완료 상태가 그대로 보인다.
- 브라우저 탭을 닫았다가 다시 열어도 이전에 입력했던 할 일들이 남아있다.
- 이 시점에서 PRD의 MVP 범위(할 일 추가, 카테고리/중요도/마감일 설정, 완료 체크/삭제, 완료율 표시, 새로고침 후 유지)가 모두 충족된 완성된 형태의 웹앱이 된다.

---

## 4단계 (확장): 시작일 ~ 마감일 기간 지정

### 이 단계에서 하는 일 (쉬운 설명)
지금까지는 할 일마다 "언제까지"(마감일) 하나만 정할 수 있었다. 이번 단계에서는 "언제부터 언제까지"인지, 즉 시작일과 마감일을 함께 정할 수 있도록 입력 화면과 데이터를 바꾼다. 아직 달력으로 보는 화면은 만들지 않고, 지금의 목록 화면에 기간 정보가 잘 표시되는 것까지만 다룬다.

### 포함되는 요구사항 (PRD.md 9.2-1)
- 할 일 등록 시 시작일과 마감일(종료일)을 함께 입력 (시작일은 선택 사항으로 남겨 기존 방식과도 호환)
- 시작일이 마감일보다 늦으면 안 된다는 최소한의 확인
- 목록 화면에서 "n월 n일 ~ n월 n일" 형태로 기간이 표시되도록 수정

### 왜 이 단계를 먼저 하는가
5단계에서 만들 달력 화면은 "이 할 일이 어느 날짜부터 어느 날짜까지인지"를 알아야 막대로 그릴 수 있다. 그 정보(시작일~마감일)를 먼저 데이터와 입력 화면에 만들어 두어야, 다음 단계에서 달력에 자연스럽게 이어붙일 수 있다.

### 이 단계가 끝났을 때 확인할 수 있는 모습
- 할 일 등록 폼에 마감일 외에 시작일 입력란이 추가되어 있다.
- 시작일 없이 마감일만 입력해도 기존처럼 정상 등록된다.
- 시작일을 마감일보다 늦게 입력하면 안내 메시지가 뜨고 등록되지 않는다.
- 목록에서 각 할 일의 기간이 "시작일 ~ 마감일" 형태로 보인다.
- 1~3단계에서 만든 기능(추가/카테고리/중요도/완료 체크/삭제/완료율/새로고침 유지)은 그대로 동작한다.

---

## 5단계 (확장): 리스트 ↔ 달력 전환 & 기간 시각화

### 이 단계에서 하는 일 (쉬운 설명)
화면 상단에 "목록으로 보기 / 달력으로 보기" 버튼을 추가해서 원하는 대로 화면을 바꿔볼 수 있게 한다. 달력으로 보기를 누르면 한 달짜리 달력이 나타나고, 각 할 일이 시작일부터 마감일까지 막대(바) 형태로 달력 위에 표시된다.

### 포함되는 요구사항 (PRD.md 9.2-2, 9.2-3)
- 리스트 보기 ↔ 달력 보기 전환 버튼(또는 탭)
- 달력 보기: 한 달 단위 달력에 할 일을 시작일~마감일 범위의 막대로 표시
- 카테고리별 색상 구분, 완료 항목의 시각적 구분(흐림/취소선 등)을 달력에서도 동일하게 적용
- 하루짜리 할 일(시작일=마감일 또는 마감일만 있는 경우)은 그 날짜 한 칸에 표시

### 왜 이 단계를 나중에 하는가
달력에 기간을 막대로 그리려면 각 할 일에 시작일과 마감일 정보가 이미 있어야 한다(4단계 결과물). 또한 리스트 보기 자체는 1~3단계에서 이미 완성되어 있으므로, 이 단계는 "같은 데이터를 다른 방식(달력)으로도 보여주는 화면"을 추가하는 데 집중하면 된다.

### 이 단계가 끝났을 때 확인할 수 있는 모습
- 화면에서 "목록 보기 / 달력 보기" 버튼을 눌러 두 화면을 자유롭게 오갈 수 있다.
- 달력 보기에서 이번 달 달력이 보이고, 각 할 일이 시작일부터 마감일까지 걸쳐 막대로 표시된다.
- 학과/동아리/개인 할 일이 달력에서도 색상으로 구분된다.
- 완료된 할 일은 달력에서도 흐리게 표시되거나 취소선이 있는 등 구분된다.
- 달력 보기에서도 완료 체크와 삭제가 가능하다.
- 리스트 보기의 기존 기능(추가/완료 체크/삭제/완료율/새로고침 유지)은 그대로 동작한다.

---

---

## 6단계 (v1.2 확장): UX 고도화 & 완성도 개선

### 이 단계에서 하는 일 (쉬운 설명)
5단계에서 완성한 달력+목록 분할 화면 위에, 실제 사용에서 필요한 다양한 기능들을 한꺼번에 추가한다. 할 일을 찾고 걸러내는 검색·필터·정렬, 내용을 바로 수정하는 인라인 편집, 마감 긴박도를 알려주는 D-day 표시, 눈의 피로를 줄이는 다크 모드, 카테고리와 중요도를 직접 관리하는 설정 모달, 그리고 다른 기기로 데이터를 옮기는 내보내기/가져오기까지 추가하여 앱을 완성도 높은 형태로 마무리한다.

### 포함되는 요구사항 (PRD.md 10.2)
- 달력 + 목록 동시 보기 (데스크톱: 분할 레이아웃, 모바일/태블릿: 탭 전환)
- 달력 ↔ 목록 연동 (항목 클릭 → 달력 월 이동·강조 / 달력 막대 클릭 → 목록 항목 선택)
- 실시간 검색 (제목·메모 기준, 달력과 목록에 동시 반영)
- 필터 (카테고리·중요도·완료 여부) & 정렬 (등록순·마감일순·중요도순)
- 인라인 수정 (목록에서 직접 편집)
- 메모 필드 추가
- D-day 표시 및 긴급도 색상 구분
- 카테고리별 진행률 바
- 다크 모드 (버튼 전환, localStorage 저장)
- JSON 내보내기/가져오기 (할 일 + 카테고리·중요도 설정 포함)
- 설정 모달: 카테고리·중요도 항목 추가/삭제 커스터마이징

### 이 단계가 끝났을 때 확인할 수 있는 모습
- 데스크톱에서 달력(좌)과 목록(우)이 한 화면에 동시에 보인다.
- 목록 항목을 클릭하면 달력이 해당 월로 이동하고 해당 셀이 강조된다.
- 달력 막대를 클릭하면 목록에서 해당 항목이 선택되고 스크롤된다.
- 검색창에 입력하면 목록과 달력 모두 해당 항목만 표시된다.
- 카테고리·중요도·완료 여부 필터와 정렬이 모두 동작한다.
- 목록에서 ✏️ 수정 버튼을 누르면 해당 항목이 편집 폼으로 바뀐다.
- 각 할 일에 D-day 배지가 표시되고, 임박·당일·초과 여부가 색상으로 구분된다.
- 카테고리별 진행률 바가 전체 완료율 아래에 표시된다.
- 🌙/☀️ 버튼으로 다크 모드를 전환할 수 있고, 새로고침해도 유지된다.
- ⬇ 내보내기 버튼으로 JSON 파일을 다운로드할 수 있고, ⬆ 가져오기로 복원할 수 있다.
- ⚙️ 설정에서 카테고리와 중요도를 추가하거나 삭제할 수 있다.

---

## 참고: 이번 범위에 포함하지 않는 것

### 전체 공통 제외 범위
- 회원가입/로그인, 서버·DB 연동, 실시간 다기기 동기화
- 마감일 알림/푸시 알림
- 통계(차트) 기능
- 반복 일정, 협업 기능

### 달력 관련 제외 범위 (PRD.md 9.3)
- 여러 달을 한 번에 보여주는 연간 보기
- 달력에서 날짜를 드래그해서 새 할 일을 만드는 기능
- 주간(week) 단위 보기
